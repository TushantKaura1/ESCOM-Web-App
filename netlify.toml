[build]
  command = "ls -la && echo 'Using pre-built files' && ls -la dist/"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
